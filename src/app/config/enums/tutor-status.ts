export enum TutorStatus {
  online = 'online',
  offline = 'offline',
  rejected = 'rejected',
  pending = 'pending',
  suspended = 'suspended',
  active = 'active',
  deactive = 'inactive',
  suspend = 'suspend',
}
