import { DefaultAvatarDirective } from './default-avatar.directive';

describe('DefaultAvatarDirective', () => {
  it('should create an instance', () => {
    const directive = new DefaultAvatarDirective();

    expect(directive).toBeTruthy();
  });
});
