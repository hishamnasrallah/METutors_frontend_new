import { DefaultCourseDirective } from './default-course.directive';

describe('DefaultCourseDirective', () => {
  it('should create an instance', () => {
    const directive = new DefaultCourseDirective();

    expect(directive).toBeTruthy();
  });
});
