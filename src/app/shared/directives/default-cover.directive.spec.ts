import { DefaultCoverDirective } from './default-cover.directive';

describe('DefaultCoverDirective', () => {
  it('should create an instance', () => {
    const directive = new DefaultCoverDirective();

    expect(directive).toBeTruthy();
  });
});
